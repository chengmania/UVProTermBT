UVProTermBT — Session Log
=========================

2026-07-13 — Phase 0/1 (project pivot from AXTermPuter, scaffold + KISS codec)
- New sibling project to AXTermPuter (~/Dev/Projects/AxTermPuter). Same
  feature scope (APRS messenger + AX.25 terminal for the UV-Pro), but as a
  Python desktop/laptop app instead of Cardputer firmware, because the
  AXTermPuter investigation this session confirmed the UV-Pro's real
  KISS/TNC data path is classic Bluetooth RFCOMM, which the Cardputer's
  ESP32-S3 (BLE-only radio) cannot reach at all. Full findings carried
  over into this repo's docs/PROTOCOL.md §2. Laptops/tablets (Chuwi
  MiniBook, HiMax10, both Kubuntu) have real classic-BT radios, so this
  project can talk to the radio's actual transport directly.
- Created CLAUDE.md, ROADMAP.md, docs/PROTOCOL.md, LOG.txt.
- Decisions: Python 3.12+; classic BT via stdlib
  `socket.AF_BLUETOOTH`/`BTPROTO_RFCOMM` (no extra dependency, Linux-
  native); urwid for the terminal UI; pytest for tests; PyInstaller +
  GitHub Actions for Linux (primary) and Windows (stretch) executable
  releases. All daily-use devices are Kubuntu — don't let Windows
  compatibility block Linux progress.
- Added uvprotermbt/kiss.py: direct port of AXTermPuter's native-tested
  lib/kiss/kiss_codec.{h,cpp} — encode_frame() and streaming KissDecoder
  with the same design decisions (empty-frame skip, malformed-escape
  resync).
- Added tests/test_kiss.py: ported the same 8 cases from AXTermPuter's
  test/test_kiss/test_kiss_codec.cpp (encode escaping, single/multi-frame
  decode, empty-frame skip, fragmentation at every split point, malformed-
  escape resync, 300-byte payload, deterministic fixed-seed fuzz test).
- VERIFIED: `pytest tests/` PASSED (8/8), via a local .venv.
- NOT done: git push to github.com/chengmania/UVProTermBT (repo has no
  commits yet).
- NEXT: Phase 2 — RfcommKissLink (classic BT RFCOMM connect from Linux,
  background RX thread + queue, reconnect logic), confirm the SDP-
  advertised RFCOMM channel from this machine directly (channel 1 worked
  for one Android app, channel 4 for another — need our own first-hand
  confirmation, not just inferred from phone captures), then a HARDWARE
  TEST decoding real live KISS/AX.25 frames from the UV-Pro.

2026-07-13 — Phase 2 (RFCOMM link) — implemented, channel corrected, still flaky live
- Added uvprotermbt/link.py: RfcommKissLink (socket.AF_BLUETOOTH/
  BTPROTO_RFCOMM), background RX thread + queue, send()/on_receive()/
  poll(), reconnect with backoff. Added scripts/monitor.py as a Phase-2
  hardware smoke test (connect, print raw + KISS-decoded frames).
- Confirmed via `sdptool records <addr>` (not `browse`, which comes back
  empty) from this machine directly: SDP shows RFCOMM channels 2 ("BS
  AOC"), 3 (Voice Gateway/HFP), 4 ("SPP Dev"/Serial Port 0x1101).
  Initially targeted channel 4 as the "proper" SDP-discoverable choice.
- CORRECTION: found the khusmann/benlink project on GitHub — a Python
  library specifically reverse-engineering this exact radio family
  (Benshi: BTech UV-Pro, RadioOddity GA-5WB, Vero VR-N76/N7500, BTech
  GMRS-Pro). It documents the real BLE control service
  (00001100-d102-11e1-9b23-00025b00a5a5 — not the 00000001-ba2a-... one
  we'd found, which is itself a red herring) and a GaiaFrame/Message
  command protocol. Its own RFCOMM example uses channel 1, not 4.
  Cross-checked against our earlier captured bytes: channel 4 traffic
  ends in `...ff 01 00` (a GaiaFrame header), channel 1 traffic starts
  with `c0 00` (raw KISS FEND + command byte, no envelope). Conclusion:
  channel 4 is a red herring (generic serial profile, not the real TNC
  path); channel 1 carries genuine raw KISS pass-through. Changed
  RfcommKissLink.DEFAULT_CHANNEL from 4 to 1. Full writeup in
  docs/PROTOCOL.md §2. Decided NOT to adopt benlink's Message/GaiaFrame
  protocol for our core KISS data path — our own kiss.py + RfcommKissLink
  on channel 1 is simpler and sufficient; benlink stays a good reference
  for future radio-control features (battery, channel switching).
- VERIFIED live from Linux (twice): a bare RFCOMM connect to channel 1
  succeeds cleanly after a fresh `bluetoothctl pair`. Neither test window
  happened to have live RF traffic, so we haven't yet decoded a real
  AX.25 frame end-to-end through this project — transport-level
  connection is confirmed, full data-path confirmation is still pending.
- NOT resolved: classic BT connection reliability. Spent a large amount
  of this session on live hardware debugging — saw clean connects,
  TimeoutError, ConnectionRefusedError, and BlueZ-level
  br-connection-refused (that last one specifically from an asymmetric
  bond: our side had removed its copy of the link key while the radio
  still remembered pairing with us — symmetric removal on both sides
  fixed that particular case). The radio's own "paired device" indicator
  was observed appearing and disappearing within a few seconds,
  independent of anything we did. A full power cycle (battery pull)
  restored BLE connectivity once when it had gone flaky. Holding a BLE
  connection open at the same time does NOT stabilize classic RFCOMM —
  confirmed independent. No access to the radio's own firmware logs, so
  further root-causing from software has hit its limit for tonight.
- NEXT: get a genuine live end-to-end KISS/AX.25 decode through
  RfcommKissLink on channel 1 (needs to coincide with actual RF traffic
  during a working connection window). If classic-BT flakiness keeps
  blocking progress, consider a dedicated session focused purely on
  characterizing the radio's connection-window timing before continuing
  with Phase 3 (AX.25 parsing) against real traffic — though Phase 3's
  parsing logic itself doesn't require live hardware and could proceed
  independently using known-good captured byte sequences as test fixtures.

2026-07-13 — Phase 2 (RFCOMM link) — root cause found and fixed
- User found two independent real-world writeups documenting a working
  classic-BT KISS-TNC setup for this exact radio family (VR-N76/BTech
  UV-Pro) on Linux: a HamRadioTech article (DC6AP, Sep 2025, "VR-N76
  (UV-PRO) with KISS-TNC under Linux") and the
  islandmagic/kiss-tnc-test Raspberry Pi guide. Both independently
  confirm RFCOMM channel 1 (matching our own khusmann/benlink-informed
  finding), and both call out disabling BlueZ's Bluetooth headset/
  Hands-Free profile as a required fix we'd never tried.
- Confirmed the root cause directly on this machine via
  `journalctl -u bluetooth`: repeated, exactly-timed log lines
  throughout every flaky session tonight —
  `bluetoothd[1256]: src/profile.c:ext_connect() Hands-Free unit failed
  connect to 38:D2:00:01:38:8F: Connection refused (111)`. BlueZ's
  `hfp-hf` (Hands-Free unit/client role) plugin auto-connects to the
  radio's advertised Handsfree Audio Gateway service (SDP `Voice
  Gateway`/0x111f, channel 3), contending with our RFCOMM channel-1 KISS
  connection attempts at the BlueZ/kernel level, regardless of which
  client API initiates the connection.
- The two articles' own fix (`/etc/bluetooth/input.conf`,
  `Disable=Headset`) is stale for this system's BlueZ 5.72 — confirmed
  that option doesn't exist in this version's input.conf (HID-only).
  Found the modern equivalent: `bluetoothd --help` documents
  `-P/--noplugin=NAME`; `strings /usr/libexec/bluetooth/bluetoothd`
  confirmed compiled-in plugin names `hfp-hf`/`hfp-ag`/`hsp-hs`/`hsp-ag`.
- APPLIED (user-confirmed, system-wide change): created
  `/etc/systemd/system/bluetooth.service.d/override.conf` disabling only
  `hfp-hf` (the exact role in the failing log line):
  ```ini
  [Service]
  ExecStart=
  ExecStart=/usr/libexec/bluetooth/bluetoothd --noplugin=hfp-hf
  ```
  Applied via `sudo systemctl daemon-reload && sudo systemctl restart
  bluetooth`; verified new bluetoothd process running with the flag.
  A2DP (music/audio playback) endpoints still register normally —
  unaffected. Rollback: remove the override.conf, daemon-reload, restart.
- Updated pairing procedure to include `trust <MAC>` after `pair <MAC>`
  (both external articles use it, we never had) — documented in
  docs/PROTOCOL.md §2 and scripts/monitor.py's docstring.
- Architecture decision: keep RfcommKissLink's raw
  socket.AF_BLUETOOTH approach rather than switching to `rfcomm bind` +
  pyserial (which both external guides use) — the root cause was at the
  BlueZ/kernel level below either client API, so their reliability is
  attributable to the headset fix, not to `rfcomm bind` itself. No
  proven benefit to switching; would add a sudo-gated system device
  node, a new pyserial dependency, and a duplicate reconnect lifecycle.
  Documented as a fallback option if raw sockets are still unreliable
  after this fix.
- Polished uvprotermbt/link.py: differentiated ConnectionRefusedError/
  TimeoutError/generic OSError in _run()'s logging for actionable
  diagnostics (still same backoff behavior, no control-flow change);
  added a threading.Lock around _sock/_connected mutation in send()/
  _handle_disconnect() (defensive cleanup for a previously-benign race
  between the main thread's send() and the RX thread's own disconnect
  handling); send() failures are now logged instead of silent.
- VERIFIED: `pytest tests/` still 8/8 passing after the link.py changes.
- Documented direwolf's TX limitation with this radio (HamRadioTech:
  direwolf RX works, TX silently fails, cause unknown; recommends
  TFKISS) — noted for later Phase 4+ on-air TX testing, not installing
  new software this session since our current hardware test is RX-only.
- NEXT: live hardware re-test with the fix in place — re-pair with
  `remove` + `trust`, run scripts/monitor.py, confirm a connection that
  stays up (not flapping), and catch a real live KISS/AX.25 decode
  during actual RF traffic to finally close out the Phase 2 hardware
  test.

2026-07-14 — Phase 2 CLOSED: first live KISS/AX.25 decode; transport rewritten
- **Milestone: decoded live APRS traffic end-to-end for the first time.**
  Caught real Mic-E position beacons from the UV-Pro over Bluetooth:
  `KC3SMW-7 > <Mic-E dest> via WIDE1-1,WIDE2-1`, control 0x03 / PID 0xF0,
  raw KISS framing (`c0 00 ... c0`, no GaiaFrame envelope).
- **The raw-socket approach was a dead end — rewrote the transport.** The
  prior `RfcommKissLink` (raw `socket.AF_BLUETOOTH` to channel 1) never
  works with this radio: when the radio's BR/EDR is awake it *refuses*
  bare channel connects on every channel (swept 1–15), and it power-saves
  between pages so connects otherwise time out. Proved this session that
  the earlier "cat /dev/rfcomm0 held 60s = healthy link" reading was
  wrong — the open was just blocking on a connect that never completed
  (a direct socket connect test made it explicit: TimeoutError). Credit
  to Greg for pushing back with "I don't think you ever connected" — that
  was correct and redirected the whole session.
- **Root method (the fix): BlueZ SerialPort profile, not a raw socket.**
  The radio only serves KISS through its advertised SerialPort profile
  (UUID 0x1101) via an SDP-negotiated *profile* connection — same as YAAC
  on macOS and WoAD on Android. A groups.io YAAC/VR-N76 guide from Greg
  named the missing radio-side step (Enable KISS TNC) and confirmed the
  OS-mounts-a-serial-port model. Implemented on Linux by registering an
  `org.bluez.Profile1` (client role) over D-Bus and reading the RFCOMM fd
  BlueZ hands to `NewConnection`. Pure Python, no sudo, no `rfcomm` tool,
  no pyserial.
- **Two required prerequisites nailed down this session:**
  1. **Enable KISS TNC on the radio** (Settings → General Settings →
     KISS TNC → Enable KISS TNC). Not in the Rev3 PDF manual; it's a
     current-firmware feature. Until on, the profile connect is refused.
  2. **Clean symmetric bond.** The persistent pairing failure
     (`AuthenticationRejected` / `br-connection-refused`) was a stale link
     key in `/var/lib/bluetooth` that `bluetoothctl remove` silently
     skips when the device object isn't present. Fix: `sudo rm -rf` the
     device's bond dir + `systemctl restart bluetooth`, forget on the
     radio too, then pair+trust. Let KDE's bluedevil be the pairing agent
     (registering our own fails — one already exists).
- **Rewrote `uvprotermbt/link.py`** onto the profile method, keeping the
  exact public API (begin/stop/is_connected/send/on_receive/poll). Runs a
  GLib main loop on its own thread; `Disconnect()`-before-`Connect()` to
  clear `br-connection-busy`; treats `NoReply`/busy as transient (the
  `NewConnection` callback is the real success signal); backoff reconnect;
  reads the fd via a GLib IO watch into the existing rx queue. Guarded
  dbus/gi import so the package still imports (and codec tests still run)
  on hosts without the BlueZ bindings.
- **Rewrote `scripts/monitor.py`** as the reproducible smoke test (defaults
  to the UV-Pro MAC, prints decoded frames with a source>dest-via-path
  AX.25 summary). VERIFIED live: it connects via the profile and streams
  decoded beacons.
- **Build change:** the venv now needs `--system-site-packages` for the
  system `python3-dbus`/`python3-gi`. Recreated the venv that way;
  documented in requirements.txt, CLAUDE.md, and docs/PROTOCOL.md §3.
  `pytest tests/` still 8/8 green.
- **Docs:** docs/PROTOCOL.md gets a new §3 "Reproducing the Live KISS
  Link — the working method" (full recipe + gotchas), the old §2
  raw-socket/channel-1 guidance marked SUPERSEDED, sections renumbered
  (AX.25 §4, APRS §5, Callsign §6, Legal §7). ROADMAP Phase 2 closed.
- **Known follow-ups (Phase 3):** payloads are **Mic-E** encoded, so
  `aprs.py` must decode Mic-E (destination-address lat + `` ` ``/`'` info
  type), not just standard `!`/`=`/`:`. Also: the transport shows one
  brief disconnect/reconnect right after first connect (the
  Disconnect-first clears state, then it stabilizes) — acceptable, but
  worth smoothing later so the initial connect doesn't flap once.
- **TX WORKS TOO (same session): transmitted an APRS message off-air.**
  Built an AX.25 UI frame by hand (KC3SMW-7 > APZUVT, direct/no path,
  info `:KC3SMW-9 :hello from UVProTermBT{1`), KISS-framed it, sent it via
  `link.send()`. direwolf (monitoring off-air) decoded it cleanly:
  `KC3SMW-7>APZUVT::KC3SMW-9 :hello from UVProTermBT{1` — parsed as an
  APRS Message, number "1". So the link is fully bidirectional: RX (live
  Mic-E beacons + a message ack) and TX both confirmed on real RF. Note
  the direwolf-TX-fails caveat in PROTOCOL.md §2 does NOT apply — that's
  direwolf transmitting; here direwolf only receives, and WE transmit via
  the UV-Pro, which works.
- **Folded the validated encoder into `uvprotermbt/ax25.py`** (first real
  Phase 3 module): `encode_ui_frame()` (control 0x03 / PID 0xF0) plus
  `decode_frame()` for the address field (source > dest via path) and info.
  `Address` dataclass with proper SSID-byte handling — including the
  subtlety that bit 7 is the command/response (C) bit on dest/source but
  the has-been-repeated (H) bit on digipeaters (caught by a failing test
  where an encoded dest round-tripped as "repeated").
- **Tests: `tests/test_ax25.py` (7 tests, all green; 15 total with KISS).**
  Anchored with two real byte vectors: the exact frame direwolf accepted
  on-air (regression guard on the encoder) and a real received Mic-E
  beacon (decode guard). `scripts/monitor.py` now uses `ax25.decode_frame`
  for its summary instead of duplicating the logic.
- **Community guide + standalone demo written and validated.** Added
  `docs/UVPRO_N76_KISS_LINUX.md` (shareable, app-agnostic writeup of the
  whole method: why raw sockets fail, the SerialPort-profile approach,
  pairing/stale-key fix, troubleshooting table, credits) and
  `scripts/uvpro_kiss_demo.py` (self-contained, dependency-free-beyond-
  system-BlueZ-bindings RX/TX reference). Both RX and TX of the demo
  script VERIFIED live with direwolf awake: RX decoded live Mic-E beacons;
  TX sent `KC3SMW-7>APZUVT:>...` decoded off-air as a Status Report.
  Debugging the demo surfaced two things every user will hit, now handled
  in the script + documented: `AutoConnect: True` is mandatory on the
  profile registration (else NewConnection never fires when the desktop
  has already auto-connected the device), and the first write after
  connect can fail with Errno 103 due to the initial disconnect flap —
  retry on reconnect. (Also a testing note: direwolf's receiver can go to
  sleep and silently drop packets — verify it's still decoding the
  reference beacons before concluding a TX didn't go out.)
- NEXT: Phase 3/4 — APRS layer (`aprs.py`): Mic-E decode (the beacons are
  Mic-E), standard `!`/`=` position + `:` message parse, and the message
  ack/retry queue. The ax25 encode/decode is now a solid base; validate
  aprs.py against real off-air frames via the live link.

2026-07-14 — Application layers + GUI (APRS layer, config, then PyQt6 app)
- **Built the APRS + config layers** on top of ax25/kiss/link:
  - `aprs.py`: message/ack encode (`:ADDR:text{id`, 67-char cap), packet
    classifier (message/ack/reject/position/status/Mic-E/other), and a
    heard-stations table. `tests/test_aprs.py` (7 tests) incl. round-trips
    and the real on-air message + Mic-E beacon vectors. Mic-E is classified
    and its comment surfaced; full lat/lon Mic-E decode still TODO.
  - `config.py`: JSON Settings (callsign, ssid, bt_mac, aprs_path, theme,
    beacon) persisted to the user config dir.
- **First built a urwid terminal UI (`ui.py`)** — then learned it was the
  wrong interpretation. Greg wanted a *standalone windowed GUI resembling
  OpenWave* (which is a PyQt6 desktop app), not an in-terminal TUI. "Terminal
  style" meant the aesthetic, not the medium.
- **Pivoted to a PyQt6 desktop GUI** (`uvprotermbt/gui/`), removing the urwid
  `ui.py`. All backend layers (link/kiss/ax25/aprs/config) were UI-agnostic
  and reused unchanged — only the presentation layer changed.
  - `theme.py`: OpenWave's exact palette (navy #1a1a2e, cyan #4fc3f7,
    yellow/green/red) for dark, plus a matched light theme; Qt stylesheet.
  - `main_window.py`: OpenWave-style status bar (callsign accent + ●/○ BT
    indicator), four mode tabs (Chat/APRS/BBS/Winlink), `[MYCALL]:` input
    bar. Chat + APRS Monitor wired to the live link via a QTimer draining
    link.poll() on the Qt main thread (thread-safe GUI updates). Chat sends
    APRS messages to a `/to`-settable target, auto-acks messages addressed
    to us, renders `>>>` sent / `<call>:` RX / `*** ` sys like OpenWave.
    Theme toggle re-renders all messages with the new palette.
  - `settings_dialog.py`: callsign/SSID/MAC/path/theme editor.
  - `__main__.py`: `python -m uvprotermbt` now boots the Qt app.
- VERIFIED headlessly (QT_QPA_PLATFORM=offscreen): constructed the window,
  seeded real frames, and rendered dark/light/monitor to PNG — all correct
  and OpenWave-like. Full suite 22/22 green. requirements.txt: urwid→PyQt6.
- BBS and Winlink are present as real tabbed screens that report "AX.25
  connected mode is Phase 5" on /connect — the next backend to build.
- NEXT: **Phase 5 — AX.25 connected-mode state machine** (SABM/UA, I/RR/RNR/
  REJ, DISC/DM; T1/retry; window=1 first), pure + unit-tested before wiring
  to the live link, then a BBS session view in the GUI (connect to
  ChengmaniaBPQ). This unlocks the BBS + Winlink tabs.

2026-07-14 — Phase 5: AX.25 connected mode (BBS/Winlink terminal)
- **`ax25_conn.py`: connected-mode core, pure and event-driven.** Control-
  field encode/decode (mod-8 I/S/U frames, P/F bit), connected-mode frame
  builders (SABM/DISC/UA/DM/RR/RNR/REJ/I), and `Ax25Connection` — a
  stop-and-wait (window=1) state machine. No I/O, threads, or wall-clock:
  connect()/send()/disconnect()/on_receive()/on_timer() each return a Result
  (frames to transmit, data to deliver up, state events); the caller owns the
  link and the T1 timer. Keeps it fully unit-testable per the ground rule.
  - Refactored `ax25.py` to expose `build_address_field(dest, source, path,
    command=)` (command frames set dest C=1/source C=0; responses flip) and
    made it accept digipeaters as Address OR (call, ssid) tuples — the tuple
    bug bit both the APRS and connected paths, fixed once at the root.
- **`tests/test_ax25_conn.py` (8):** control-field known values (SABM=0x3F
  etc.) + round-trips, connect handshake, data+RR ack, window-1 queueing,
  disconnect handshake, T1 retransmit-then-give-up (N2=3), out-of-sequence
  I-frame → REJ. Full suite now **30/30**.
- **Wired into the GUI BBS/Winlink tabs.** RX routing now splits UI frames
  (control 0x03 → APRS) from connected-mode frames (→ the active session,
  filtered to frames addressed to mycall). `/connect <NODE>` runs the SABM
  handshake, the node's I-frame output streams into the terminal view (new
  "raw" line style, no timestamp), typed lines go out as I-frames, `/bye`
  disconnects. A 4 s QTimer drives T1. VERIFIED end-to-end headlessly against
  a simulated node: connect → banner ("Welcome to ChengmaniaBPQ") → command
  "H" → disconnect, plus a rendered BBS screenshot.
- Winlink tab can establish the AX.25 session too; the B2F/FBB Winlink
  protocol on top is still TODO.
- App icon: Greg supplied `icon.png` (UVPro terminal + globe + Bluetooth,
  green/blue on black). Moved to `uvprotermbt/gui/resources/icon.png`, wired
  as the window/taskbar icon and a 40×40 status-bar badge (OpenWave-style).
- NEXT: **on-air test** — /connect the real ChengmaniaBPQ node (KC3SMW-10)
  over RF and read the BBS. Then Phase 5 polish (k>1 window, T1 tuning for
  1200-baud) and Mic-E lat/lon decode for the APRS monitor.

2026-07-14 — Bugfix: connected-mode connect used the APRS WIDE path
- Greg hit this connecting our app to ChengmaniaBPQ: EasyTerm connects,
  ours didn't. Root cause: `_bbs_connect` passed `settings.path_list()`
  (the APRS path, default WIDE1-1,WIDE2-1) as the SABM's digipeater path.
  WIDE aliases don't digipeat connected-mode frames, so the SABM never
  completes its path and BPQ never answers. Verified our SABM is otherwise
  byte-correct (dest C=1, source C=0, control 0x3F) — identical structure
  to EasyTerm's working frame.
- Fix: connected mode goes DIRECT by default; added `/connect NODE via
  D1,D2` for an explicit real digipeater/node path. Never uses the APRS
  path. Verified headlessly.

2026-07-15 — AX.25 2.2 spec review + connected-mode diagnostics
- Greg added the official AX.25 v2.2 spec (docs/AX25.2.2-Jul 98-2.pdf).
  Reviewed it against ax25_conn.py. Findings:
  - Our SABM encoding is correct (§6.1.2: command = dest C-bit 1 / source 0).
  - The connect→T1→UA/DM flow (§6.3.1) and DISC teardown (§6.3.4) match ours.
  - Two defaults were off-spec: N2 retries was 3 (spec default 10), T1 was
    4000 ms (spec default 3000 ms). Fixed both.
- **Key insight for the open BBS-connect bug:** §4.3.3.1/§6.3.1 — a node that
  *receives* a SABM MUST answer UA (accept) or DM (refuse). Our frame is
  byte-correct and direwolf hears it, yet the node is silent → it never
  received it → RF/frequency, not our code. The app couldn't show this before
  (DM and timeout both printed "disconnected").
- Change: ax25_conn now emits distinct outcome events — `"refused"` on DM
  (peer heard us, declined) vs `"failed"` on give-up after N2 (peer isn't
  hearing us); `"disconnected"` still for clean DISC/UA. GUI shows different,
  actionable BBS messages for each. N2→10, T1→3000 ms. `/bye` now aborts a
  pending (not-yet-connected) session instead of leaving it to retry N2×T1.
- Tests: test_ax25_conn.py updated (give-up now asserts "failed", N2=10) +
  new test_dm_response_is_refused. Full suite 31/31. Verified the GUI
  refused/failed/abort paths headlessly (offscreen).
- docs/PROTOCOL.md §8 (new): AX.25 connected-mode notes — C-bit rule,
  UA/DM-vs-silence diagnostic, spec defaults, SABME/FRMR version detection,
  direct-connect (no APRS path) rule. Committed the spec PDF.
- NEXT (Greg, at the radio): /connect KC3SMW-7 direct on the BBS frequency —
  the app now reports "refused" (node heard you) vs "isn't hearing you",
  which resolves yesterday's open question. Then: Mic-E lat/lon decode,
  optional T3 keep-alive / v2.2 SABME negotiation, Winlink B2F.

2026-07-15 — Bugfix: connected-mode I/S-frame N(R) acknowledgment (BBS deadlock)
- Greg's TXDELAY fix (10ms→default on the UV-Pro) fixed the RF connect; the
  app then connected and showed the node welcome. But sending a BBS command
  deadlocked: the node acked our frame (RR n(r)=1, and piggybacked n(r)=1 in
  its own reply I-frame) and sent its reply, yet our app kept resending frame
  0 (n(s)=0) and the node kept REJ'ing (it wanted n(s)=1).
- Root cause: `_on_iframe` processed only N(S) (sequencing), ignoring the
  N(R) acknowledgment that every I and S frame carries (AX.25 §6.4.6). So a
  piggybacked ack never cleared our outstanding frame, and REJ handling then
  resent the already-acked frame forever.
- Fix: shared `_apply_ack(nr)` called from I-frames, RR/RNR, and REJ, so any
  N(R) that covers our outstanding frame releases the window (window=1) and
  sends the next queued frame. REJ now acks first, then resends only if still
  unacked. Added `_sframe()` helper.
- Tests: test_iframe_nr_acknowledges_our_outstanding (crafted from the live
  log) + test_rej_after_ack_does_not_resend. Full suite 33/33. Verified the
  whole connect→welcome→command→reply flow end-to-end through the GUI headless.
- NOTE (RF, Greg to watch): we clearly hear the node when idle (welcome/DISC)
  but earlier logs suggest we may miss its immediate post-TX ack/reply — if
  the exchange is still flaky after this fix, look at the UV-Pro's TX tail /
  RX turnaround (same family as the TXDELAY that fixed connect).

2026-07-15 — Bugfix: half-duplex ack storm on I-frame bursts + poll response
- After the N(R)-ack fix, BBS sessions worked (connect, welcome, commands,
  real BPQ content, "Hello Greg you have 1 messages"). But listing messages
  ("lm") failed: the node streamed the list as a burst of I-frames (n(s)=3..0,
  first five p=0, last p=1). Our app showed only the FIRST and then the node
  polled RR n(r)=2 p=1 forever until it dropped us. Greg: "it buffers full and
  fails."
- Root cause (half-duplex ack storm): we sent an RR after EVERY received
  I-frame. So we'd receive n(s)=3, immediately key TX to ack it, and go deaf
  for the rest of the node's burst (n(s)=4..0) — losing them on the air.
- Fixes (AX.25 §6.4.2 / §6.2):
  - Defer acks: only send an RR for a received I-frame when it's polled
    (P=1). BPQ sets P=1 on the last frame of a window, so we stay in RX for
    the whole burst then ack it all with one RR. Out-of-sequence still REJs.
  - Answer polls: a supervisory *command* with P=1 (the node's RR poll) now
    gets an RR response with F=1 and our N(R). Must distinguish command vs
    response — added `command` (dest C-bit) to Ax25Frame/decode_frame so we
    never "answer" the node's own RR *responses* (that caused an RR loop; a
    unit test caught it).
- Tests: test_burst_defers_ack_until_polled + test_responds_to_supervisory_poll
  (both crafted from the live log). Suite 35/35. Verified the full "lm"
  burst + poll end-to-end through the GUI headless: all lines delivered, one
  ack, poll answered.
- NEXT (Greg): pull the branch and retry `lm` / longer BBS output on the air.

2026-07-15 — Bugfix: pass slash-commands through to the BBS in a session
- BBS sending works (Greg composed a message to NY3J). But the BBS ends a
  message with "/ex", and our app ate it as a local slash-command ("unknown
  command: /ex") — leaving him unable to close the message.
- Fix (gui/main_window._send): while a terminal session is active, only our
  session-control commands (/bye, /disconnect, /d, /theme) are local;
  anything else starting with "/" (e.g. /ex) is sent to the remote as data.
  When idle, all "/" input is local as before (/connect, /to, /theme).
- Verified headlessly: /ex -> "/ex\r" to the BBS, normal text sends, /bye
  still disconnects locally, /connect still works when idle. Suite 35/35.

2026-07-15 — Bugfix: T1 was free-running, causing duplicate I-frames
- Greg saw sessions degrade (frames sent twice, node REJ cascade, eventual
  "not hearing you"). Log: every I-frame after the first went out twice.
- Root cause: the GUI's T1 was a single REPEATING 3 s QTimer started once at
  connect — not restarted per transmission. So it fired on its own phase; a
  frame sent shortly before a tick got retransmitted before the ack arrived
  → duplicate → node REJ → cascade. Intermittency (bbs single, lm+ double)
  was pure timer phase. AX.25 §6.7.1.1: T1 starts when an I-frame is sent and
  stops when acked.
- Fix: T1 is now single-shot, (re)armed from a new
  `Ax25Connection.awaiting_response` predicate after every connection event
  (`_arm_t1`) — started while mid-handshake or an I-frame is outstanding,
  cancelled the instant an ack clears it. Reordered `_bbs_connect` so the
  timer exists before the connect result arms it.
- Tests: test_awaiting_response_drives_t1 + headless GUI verification (T1
  active on send, off on ack, stale fire doesn't retransmit, unacked fire
  retransmits exactly once and re-arms). Suite 36/36.

2026-07-15 — Resilience: T1 jitter + more retries; beacon/UV-Pro TX finding
- FINDING (not our code): during a BBS session, the node's digipeated ID
  beacon (KC3SMW-7>ID,W3SK: ...) congests the shared half-duplex channel and
  the UV-Pro's KISS TNC then "refuses to send packets" — its transmit path
  wedges. Verified by simulation that our app handles the beacon correctly
  (it's a UI frame -> APRS monitor; the connected-mode session state and the
  following ack are untouched). So this is a UV-Pro TNC / RF-contention issue,
  not a protocol bug. Greg can't/won't change the node (it works with other
  clients), so the fix on our end is resilience, not correctness.
- Changes (AX.25 §6.3.6.1 collision recovery, §6.7.1.1):
  - T1 retransmit now has random jitter (base 3 s + up to 2 s) so repeated
    retries don't collide at the same phase during contention. Jitter lives
    in the GUI's _arm_t1 (the state machine stays deterministic/testable).
  - N2 15 (was 10) for extra patience so a burst of beacon/contention doesn't
    drop the link before it clears.
- Verified headlessly: T1 intervals spread across 3000-5000 ms, cancelled on
  ack; N2=15. Suite 36/36 (the N2 give-up test reads _MAX_RETRIES).
- Still limited by the UV-Pro's TX behavior under channel contention — if the
  radio's KISS TX wedges, retries can't help. Worth noting for users: keep
  the UV-Pro's own APRS beacon OFF while using it as a KISS TNC, and prefer a
  quieter frequency for connected-mode/BBS work.

2026-07-15 — Auto-recover a wedged KISS link (no app restart needed)
- KEY finding from Greg: after the wedge, the radio only starts sending again
  when he closes and REBOOTS THE PROGRAM — not the radio. UV-Pro's own beacon
  is off. So the wedge is on OUR side: the RFCOMM link gets stuck (os.write()
  keeps 'succeeding' but nothing reaches the radio, no error), and only a
  fresh RFCOMM connection clears it. An app restart re-establishes that
  connection; nothing else did.
- Fix: `RfcommKissLink.reconnect()` — forces a fresh RFCOMM connection
  (close fd + Disconnect/Connect -> new fd via the profile's NewConnection),
  i.e. what an app restart does, but in-place. The GUI now calls it on a
  session stall ("failed" event) so the wedge clears automatically; the user
  just waits for ● BT and /connect again — no restart.
- Verified headlessly (stall -> link.reconnect() invoked once, session
  cleared, message shown). Suite 36/36.
- CAVEAT: diagnosed from Greg's description, not direct observation of the
  wedge. If reconnect() alone doesn't clear it on hardware, the stronger
  version is to unregister+re-register the BlueZ profile (or bounce the whole
  link thread) — closer to a full restart. Needs on-air confirmation.

2026-07-16 — Phase B: first-run setup + safe defaults (shareable build)
- Problem: defaults shipped Greg's callsign (KC3SMW-7) and radio MAC, so a
  fresh install would transmit as someone else's call (Part 97 problem) with
  no way to find the user's own radio.
- config.py: neutral defaults (callsign N0CALL, ssid 0, empty bt_mac) +
  is_configured() — true only with a real callsign AND a radio MAC. Transmit
  is gated on it.
- bt.py: BlueZ D-Bus device discovery (list known devices fast; optional
  StartDiscovery). Radios (UV-PRO/VR-N/…) sort first. Verified it finds the
  paired UV-Pro.
- gui/radio_picker.py: RadioPicker dialog (scan off the UI thread, pick a
  device). gui/setup_wizard.py: first-run wizard (callsign + radio + the
  Enable-KISS-TNC/pairing checklist). settings_dialog gains a "Select…"
  button; __main__ runs the wizard when unconfigured.
- main_window: Radio menu (Select Radio…, Reconnect Link); TX and /connect
  gated on is_configured(); link not started without a MAC; changing the
  radio rebuilds the link (_rebuild_link).
- Verified headlessly: unconfigured -> TX blocked + "not configured" shown +
  link not started; configured -> TX gate passes; all dialogs construct;
  wizard renders correctly. Suite 36/36.

2026-07-16 — Phase A: UI improvements (history, heard panel, session badge)
- gui/widgets.py HistoryLineEdit: Up/Down command-history recall (shell-like)
  on the input line; main_window remembers each submitted line.
- APRS tab is now a splitter: monitor scrollback + a heard-stations panel
  (call, last kind, age, count), fed from aprs.HeardTable on every RX.
  Double-click a station -> sets it as the chat target and jumps to Chat.
- Status bar gains a BBS session badge: "BBS: —" / "connecting…" / "● <node>"
  (green when connected), refreshed on every connection event and theme change.
- Verified headlessly (history recall cycles; heard list populates + click
  sets target; badge idle/connecting/connected) and rendered. Suite 36/36.

2026-07-16 — Fresh-tablet fixes: in-app pairing + private D-Bus bus
- Greg loaded the setup-and-ui build on the Chuwi tablet (a true "not me,
  fresh machine" test). Install + wizard fine, but the radio didn't connect.
  Two real program bugs (fix the program, not the machine):
  1. The radio picker only recorded the MAC — it never PAIRED the tablet to
     the radio. The KISS link connects via the SerialPort profile, which needs
     a bond, so an unpaired-but-selected radio never connects. Worked on the
     dev box only because the UV-Pro was already paired there.
     FIX: bt.pair()/is_paired() (Device1.Pair + Trusted, friendly errors);
     RadioPicker pairs an unpaired device on select (off-thread, "put the
     radio in Pairing mode", success/failure feedback) and won't "finish" on
     an unpaired device. Labels show paired / "not paired (will pair on
     select)". main_window warns if it starts with an unpaired MAC.
  2. bt.py used the shared dbus.SystemBus() singleton. Called from the wizard
     BEFORE link.py sets up its GLib main loop, that singleton is created
     without a loop, and link's profile registration then fails with "D-Bus
     connections must be attached to a main loop" — i.e. the wizard could break
     the very link it just configured. FIX: bt.py uses PRIVATE bus connections
     (dbus.SystemBus(private=True), closed after use) so it never pollutes the
     singleton the link relies on. Reproduced the failure and confirmed the fix.
- Relies on the desktop BT agent (KDE bluedevil on the Chuwi) for the
  Just-Works confirmation. Suite 36/36.

2026-07-16 — Phase C: Winlink via PAT (KISS-over-TCP bridge)
- Greg's question — "can we AGWPE with a KISS TNC over BT rather than a BT
  serial connection?" — led to a much simpler architecture than the original
  AGWPE-server idea. Found that pat-gensio (PAT with built-in AX.25) connects
  to a KISS TNC over TCP directly (gensio `kiss,tcp,localhost,8001`). So we
  don't implement AGWPE OR do AX.25 for Winlink — PAT does it all; we just
  re-serve the radio's KISS stream on a local TCP socket.
- uvprotermbt/kiss_tcp.py: KissTcpServer — a transparent byte pipe on top of
  the existing BT link. Background accept/read loop forwards PAT→radio via
  link.send(); the GUI feeds radio→PAT via feed_from_radio() from its RX poll.
  One client (one radio), thread-safe, no GUI-thread I/O.
- Winlink tab: Start/Stop Winlink Bridge button + the exact PAT config string
  (kiss,tcp,127.0.0.1,8001). While bridging, the app relays RX to PAT, still
  decodes for the monitor, and BLOCKS its own TX (PAT owns the radio). Poll
  detects PAT connect/disconnect. Stops on close.
- Chose KISS-over-TCP over AGWPE: ~120 lines vs a full AGWPE protocol + our own
  AX.25; PAT's mature stack (k>1) handles B2F better than our stop-and-wait.
- tests/test_kiss_tcp.py (3): both-direction relay, client-disconnect state,
  no-client no-op. Suite 39/39. GUI flow verified headlessly + rendered.
  docs/WINLINK_PAT.md: PAT/pat-gensio setup guide.
- NEXT (Greg, on-air): install pat-gensio, Start Winlink Bridge, point PAT at
  kiss,tcp,localhost:8001, connect to an RMS, send/receive a Winlink message.

2026-07-17 — Phase C.2: one-click "Setup & Go" Winlink (automate kissattach)
- The proven Winlink path (socat pty → sudo kissattach → kernel AX.25 → PAT
  ax25+linux) wasn't load-and-go. Automated it end-to-end from the app.
- tncattach ruled out for Winlink (makes TUN/TAP IP interfaces, not AX.25; an
  RMS can't talk to it). Its -T --kisstcp mode → our KISS-TCP bridge stays a
  future IP/Reticulum option.
- uvprotermbt/pty_bridge.py (new): PtyBridge — native pty (os.openpty), no
  socat. Relays the pty MASTER ↔ radio; kissattach sets N_AX25 on the SLAVE, so
  our master relay is exactly the KISS-TNC interface the kernel wants. Holds the
  slave fd to avoid EIO before kissattach opens it. tests/test_pty_bridge.py (2).
- scripts/winlink-helper.sh (new): privileged helper run via pkexec (GUI
  password). Subcommands install (apt ax25-tools/libax25), attach (modprobe,
  sync one axports line, kissattach + kissparms), detach (kill kissattach +
  ip link down). Strict arg validation + safe PATH (runs as root).
- main_window Winlink tab: Start Winlink Bridge → (first time) offers install →
  pty → pkexec attach → shows PAT config (engine linux, axport wl2k). pkexec via
  QProcess (async, off the UI thread). _bridge_active still pauses app TX.
  closeEvent detaches. config.py: winlink_call (+ winlink_callsign property,
  default mycall); Settings dialog field (Winlink often uses -10).
- docs/WINLINK_PAT.md rewritten around the one-click flow; kiss_tcp.py kept +
  documented for pat-gensio/tncattach. Feasibility confirmed on-box (pkexec +
  polkit-kde agent running, ax25-tools in apt). Suite 41/41; GUI flow verified
  headlessly (pkexec mocked) + rendered.
- NEEDS ON-AIR (Greg): fresh box → Start Winlink Bridge → install prompt →
  password → kissattach up → PAT ax25+linux://wl2k/RMS → pull messages, no
  manual socat/kissattach. Verify kissattach returns (doesn't block QProcess).
